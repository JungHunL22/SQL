-- 코드를 입력하세요
SELECT USER_ID,NICKNAME,SUM(PRICE) TOTAL_SALES from uSED_GOODS_BOARD A LEFT JOIN USED_GOODS_USER B ON A.WRITER_ID=B.USER_ID WHERE STATUS='DONE' GROUP BY 1 HAVING SUM(PRICE)>=700000 ORDER BY 3 